// Extended types for a real identity verification orchestrator
// Includes: WorkflowDefinition (from YAML), WorkflowContext (runtime data),
// Decision/Verdict, ServiceConfig (auth), DB types (Instance, Event)
